import { useState } from 'react'
import AppContext from '../contexts/AppContext';

function App() {
  const [count, setCount] = useState(0)

  return (
    <AppContext>
		</AppContext>
  )
}

export default App
